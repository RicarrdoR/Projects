import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'curso1FullStack',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
